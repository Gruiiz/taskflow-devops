[project]
name = "taskflow-api"
version = "1.0.0"
description = "API educacional para demonstrar práticas de DevOps"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]
